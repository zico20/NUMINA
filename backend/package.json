{
  "name": "smart-calc-backend",
  "version": "1.0.0",
  "description": "Proxy server for Smart Calc app — keeps OpenAI API key off the device.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "openai": "^4.73.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {}
}
